<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authFragment">

    <fragment
        android:id="@+id/authFragment"
        android:name="com.akumakeito.vyyer.presentation.auth.AuthFragment"
        android:label="AuthFragment" >
        <action
            android:id="@+id/action_authFragment_to_scanHistoryFragment"
            app:destination="@id/scanHistoryFragment" />
    </fragment>
    <fragment
        android:id="@+id/scanHistoryFragment"
        android:name="com.akumakeito.vyyer.presentation.scanhistory.ScanHistoryFragment"
        android:label="ScanHistoryFragment" />
</navigation>